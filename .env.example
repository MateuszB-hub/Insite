# Copy to .env (gitignored) before `docker compose up -d`.
# Generate a password with:  python3 -c "import secrets;print(secrets.token_urlsafe(32))"
POSTGRES_USER=insite
POSTGRES_DB=insite
POSTGRES_PASSWORD=CHANGE_ME
